-- ============================================================
-- Stripe が決済完了時に自動生成するレシート URL を保存する
-- 顧客は領収書ページからアクセスして、銀行明細との突合や
-- 経費精算に利用できる。
-- ============================================================
alter table orders
  add column if not exists stripe_receipt_url text;
