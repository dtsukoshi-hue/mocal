import type { Metadata } from 'next'

export const metadata: Metadata = { title: '店舗登録 | mocal' }

export default function OnboardingLayout({ children }: { children: React.ReactNode }) {
  return children
}
